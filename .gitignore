node_modules/
scanner/node_modules/
.env
scanner/.env
*.env
out/
scanner/out/
*.pdf
# Ausnahme: anonymisierter Beispiel-Report ist ein ausgeliefertes Landingpage-Asset
!landingpage-next/public/beispiel-report.pdf
.DS_Store
*.log
# Server-spezifische Phase-1-Configs (von cloud-init geschrieben, NICHT ins Repo)
deployment/docker-compose.override.yml
deployment/.env
# Sub-Agent-Worktrees (ephemere Kopien während paralleler Arbeit)
.claude/

# Second Brain Vault (lokal, nicht committen)
vault/

# Lokale Env-Secrets + Build-Artefakte (NIE committen)
*.env.local
.next/
# Voice-Stack: ~600 MB Modelle/Binaries — lokal, gehoeren nicht ins Git
scripts/voice/

# Lokale Memory-Migrations-Kopie (nicht für Repo)
_migration/
